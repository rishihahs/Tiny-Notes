<?php
	
/*
 * Specify the type of databse that will be used
 * -- Only MySQL is supported as of now
 */

/*
 * Specify the databse server
 * eg. 'localhost'
 */
  define (db_host, 'localhost');
 
 /*
  * Specify the database to use
  */
  define (db_name, 'tinynotes');
 
 /*
  * Specify the database username to use
  */
  define (db_username, 'root');
 
 /*
  * Specify the database password
  */
  define (db_password, 'root');
 
 /*
  * Specify the database port to use
  */
  define (db_port, 3306);
  
  /*
   * Specify the site name
   */
  define(pagetitle, 'TinyNotes');

?>